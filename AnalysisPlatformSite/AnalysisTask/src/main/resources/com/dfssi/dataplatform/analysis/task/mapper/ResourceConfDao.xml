<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dfssi.dataplatform.analysis.task.mapper.ResourceConfDao">

    <insert id="insert">
        INSERT INTO server_datasource_attr(
        resource_id,
        param_name,
        param_value,
        create_date)
        VALUES (
        #{resourceId},
        #{paramName},
        #{paramValue},
        #{createDate})
    </insert>

</mapper>